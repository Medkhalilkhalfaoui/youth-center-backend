import { AuditLogBackofficeService } from './audit-log-backoffice.service';

describe('AuditLogBackofficeService', () => {
  it('should be defined', () => {
    expect(new AuditLogBackofficeService()).toBeDefined();
  });
});
