import { AuditLogBackofficeController } from "./audit-log-backoffice.controller";

 
describe('AuditLogBackofficeController', () => {
  it('should be defined', () => {
    expect(new AuditLogBackofficeController()).toBeDefined();
  });
});
